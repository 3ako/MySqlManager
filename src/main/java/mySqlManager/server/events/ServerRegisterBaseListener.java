package mySqlManager.server.events;

import java.util.EventListener;

public interface ServerRegisterBaseListener extends EventListener {
    void ServerRegisterBaseListener(ServerRegisterBase e);

}
